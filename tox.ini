[tox]
min_version = 4.0
env_list = py3{9,10,11,12}, pre-commit
passenv = LD_PRELOAD

[testenv]
groups =
    dev
    test
commands =
    test_all
    test_typing
    pc_init

[testenv:pre-commit]
skip_install = true
commands = pc_check
